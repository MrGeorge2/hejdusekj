version: '3.4'

services:
  client:
    image: client
    build:
      context: client
      dockerfile: ./debug.Dockerfile
    environment:
      NODE_ENV: development
    ports:
      - 3000:3000
      - 9229:9229
    volumes:
      - ./client:/usr/src/app
